/** Shared field styling, lifted from the existing FinalCTA input so every field reads as one system. */
export const fieldClasses =
  "w-full bg-card border border-border rounded-xl text-tx text-sm px-4 py-3.5 outline-none focus:border-secondary transition-colors placeholder:text-muted disabled:opacity-60 disabled:cursor-not-allowed";

export const fieldErrorClasses = "border-red-400/70 focus:border-red-400";

export const labelClasses = "text-xs font-semibold text-tx2 tracking-wide";
